<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Правило: видеть проект может только участник или создатель -->
    <record id="rule_project_visibility_team_only" model="ir.rule">
        <field name="name">Project: team-only visibility</field>
        <field name="model_id" ref="project.model_project_project"/>
        <field name="global" eval="True"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="False"/>
        <field name="domain_force">
            ['|', ('create_uid', '=', user.id), ('member_ids.user_id', '=', user.id)]
        </field>
    </record>

    <!-- Правило: задачи доступны если доступен проект -->
    <record id="rule_task_visibility_via_project" model="ir.rule">
        <field name="name">Tasks: visibility via project membership</field>
        <field name="model_id" ref="project.model_project_task"/>
        <field name="global" eval="True"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="False"/>
        <field name="domain_force">
            ['|', ('project_id.create_uid', '=', user.id), ('project_id.member_ids.user_id', '=', user.id)]
        </field>
    </record>

</odoo>
